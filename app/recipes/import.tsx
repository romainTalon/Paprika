/**
 * Import Recipe Screen
 *
 * Import recipe from URL using AI (JSON-LD, Claude, Vision).
 */

import { PlaceholderScreen } from "@/components/ui";

export default function ImportRecipeScreen() {
  return (
    <PlaceholderScreen
      title="Importer une Recette"
      description="Collez l'URL d'une recette pour l'importer automatiquement via IA"
      icon="🤖"
    />
  );
}
